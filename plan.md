# Civil War Like — 実装プラン

親タスク: [my-grok-task-2026#51](https://github.com/bluehive/my-grok-task-2026/issues/51)  
リポジトリ: [bluehive/civil-war-like](https://github.com/bluehive/civil-war-like)  
参考: [Ultimate General: Civil War](https://store.steampowered.com/app/502520/Ultimate_General_Civil_War/)（小規模ライク）  
マニュアル: [UGCW Game Guide v1.25 (PDF)](https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/502520/manuals/UGCW_Guide_v1.25.pdf)  
設計判断: [#1](https://github.com/bluehive/civil-war-like/issues/1)（**確定済み** — 2026-07-22）

---

## 1. ビジョン

南北戦争をモチーフにした **ターン制・六角マップ地上戦** を、**C++ 主体 + raylib（C API）** で小規模実装する。

- 北軍 vs 南軍
- 5 戦地キャンペーン（勝利数が多い方が「米国統一」）
- キーボードのみ（マウス不可）
- 見た目は PNG 最小限、**ASCII/絵文字 + 簡易図形の併用**
- 依存は少なく、mise でビルド・ローカルテスト

### 1.1 参照ゲーム要約 — Ultimate General: Civil War

出典: 公式 Game Guide v1.25（上記 PDF、約 89 頁）。**ライク対象の要約**であり、本リポは小規模・ターン制・hex・キーボード専用に簡略化する（§8 非目標参照）。

| 領域 | 参照ゲームの内容 | 本リポへの写像（方針） |
|------|------------------|------------------------|
| モード | Campaign（1861→Washington/Richmond 想定終盤）／Historical Battle／Custom Battle／Load | 5 戦地キャンペーン + 個別戦闘（Phase 5） |
| 陣営 | Union（北）vs Confederates（南） | 同左 |
| 戦場操作 | マップ移動（中ボタンドラッグ／カーソル）、ズーム、選択・移動・射撃・突撃・後退 | **hex 上**・**キーのみ**（Tab 巡回→方向キー）。リアルタイムは採用しない |
| ユニット種別 | Infantry / Cavalry / Artillery / Skirmishers + 特殊: Generals, Supply Wagon | 8 種（歩兵・騎馬・大砲・銃撃・近衛・武器/物資輸送・将軍）。散兵は銃撃/歩兵ロールに吸収 |
| 将軍 | Corps General がマップ上に存在し士気ブースト。Division/Brigade 士官は抽象。死傷の影響大 | 将軍最大 3・将軍 0 で即敗北（#1） |
| 補給 | 弾薬は有限。Supply Wagon 半径で再補給。砲は弾薬消費大 | 武器/物資輸送隊（Phase 4） |
| 状態異常 | Condition（疲労）、Morale（動揺→退却→潰走）、Shattered（戦役から消滅） | 損害 50% 敗北などへ簡略（負傷/撃破の表現は Phase 3） |
| 地形 | 草地・丘・森・密林・泥・水・浅瀬・橋・家屋/堡塁など。カバー・視界・速度・精度に影響 | 9 地形（山・丘・砂・街・道・川・平地・谷・岩）。コスト/進入は Phase 2+ |
| 勝利 | 目標地点（Objectives）確保、時間制限、戦力比バー。Mission Timer でフェーズ進行 | 期間切れ=引分／将軍0／損害50%（#1）。目標占領は後続で拡張可 |
| 戦役 | 部隊編成・士官ランク/経験・偵察レベル・セーブ分類 | 5 戦の勝敗集計で「米国統一」。セーブは JSON（Phase 6） |
| UI | ユニットカード、Army icons、ミニマップ、コマンドボタン、ガイド | テキスト + hex 図形 HUD（Phase 1 カーソル／以降拡張） |

**ガイドから拾う設計ヒント（実装時の参照用）**

- 地形は戦術の中核（高所=視界・士気・精度、森=掩蔽、水域=移動悪化）。
- 側面・背面攻撃は士気ショックが大きい → 将来の向き/隣接ルールの参考。
- 補給線・弾薬切れは射撃効率低下 → 輸送隊の価値。
- 士官喪失は軍全体に波及 → 将軍保護が勝敗に直結する設計と整合。
- 大規模リアルタイム操作（軍団 AI 委任、数十旅団）は **非目標**。小マップ・少数ユニットで「将軍視点の判断」だけを残す。

---

## 2. 開発プロセス

| ルール | 内容 |
|--------|------|
| 駆動 | GitHub Issue 駆動。実装 Issue はユーザー承認後に着手 |
| ブランチ | `main` は直接編集しない。`git worktree` で作業ブランチ |
| 区切り | 区切りの良いところで PR 提案 → 承認後に `main` へマージ |
| コミット | 作業ごとに `commit` + `push`。Issue に進捗コメント必須 |
| 環境 | Linux Mint + `mise.toml`（ビルド・テスト・watch） |
| テスト CI | GitHub Actions ではなくデスクトップ。`mise` watch + `test-log.md` |
| レビュー | 熟練 SE が push をチェック。方針変更はユーザー承認 |

### 2.1 Issue 運用（確定）

| 種別 | リポジトリ |
|------|------------|
| タスクボード親 | `my-grok-task-2026` の [#51](https://github.com/bluehive/my-grok-task-2026/issues/51) |
| 実装 Issue | **本リポ `civil-war-like`** に Phase ごとに作成 |

1. 本 `plan.md` を分割した実装 Issue を `civil-war-like` に作成
2. ユーザーが Issue を承認
3. worktree ブランチで実装 → commit / push → Issue コメント
4. 必要なら PR → 承認 → merge

---

## 3. 技術方針（確定）

| 項目 | 方針 |
|------|------|
| 言語 | **C++ 主体 + raylib（C API）** |
| 描画・入力・音 | raylib |
| hex UI | **テキスト（ASCII/絵文字）と簡易図形の併用** |
| ビルド | **mise タスクのみ**（g++ 直叩き。Makefile なし） |
| ツールチェーン | `mise.toml` で **g++ 15.2（xpack）** と **raylib 5.5** を固定 |
| セーブ | **JSON ファイル**（人間可読・差分しやすい） |
| DB | 使用しない |
| 依存 | raylib を第一目標。追加は有名ライブラリのみ |
| グラフィック | PNG は必要最小限 |

---

## 4. ゲーム設計

### 4.1 コアループ

1. キャンペーン画面で戦地選択（または順次）
2. 戦闘マップ開始（hex）
3. ターン交代（北軍 → 南軍）
4. 各ターン: 移動 / 攻撃 / 補給 / 撤退
5. 勝敗判定（§4.4）
6. 結果記録 → 次戦地 or キャンペーン終了（5 戦の勝敗で統一）

### 4.2 ユニット種別（8種）

| ID | 種別 | ロール（確定方針） |
|----|------|--------------------|
| INF | 歩兵 | **汎用**（近接・射撃のバランス） |
| CAV | 騎馬隊 | 高移動・突破 |
| WAG_W | 武器輸送隊 | 弾薬補給 |
| WAG_S | 物資輸送隊 | 食料・補給 |
| ART | 大砲隊 | 遠距離・低機動 |
| RFL | 銃撃隊 | **射撃専門・近接弱い**（歩兵との差はここ） |
| GRD | 近衛兵 | 将軍護衛・高防御 |
| GEN | 将軍 | 最大 3 人／陣営。指揮・敗北条件に関与 |

**歩兵 vs 銃撃隊（Issue #1 確定）**

- 銃撃隊: 射撃向き（射程・射撃火力を優遇）、近接戦闘は弱い
- 歩兵: 汎用（射撃も近接もそれなり）

数値（移動力・射程・攻撃・防御・コスト）の表は Phase 4 の実装 Issue で確定する。

### 4.3 マップ・地形

- **六角形（hex）** グリッド
- 地形: 山・丘・砂地・街・道路・川・平地・谷間・岩地
- 色と文字/模様で識別（テキスト + 簡易図形）

### 4.4 勝敗（確定）

毎ターン終了時および期間到達時に判定。

| 優先度 | 条件 | 結果 |
|--------|------|------|
| 1 | **期間切れ**（ターン上限到達） | **引き分け**（損害比較による勝ちは採用しない） |
| 2 | **将軍 0 人** | **即敗北** |
| 3 | **損害 50% 以上**（死亡+負傷） | **敗北** |

- 期間のモデル: **史実の日数を簡略化したターン数**（戦地ごとに設定。Phase 5 で数値表）
- キャンペーン: 5 戦地で勝利数が多い側が「米国統一」
- キャンペーン同数・戦闘引き分けの扱い: Phase 5 で詳細化（暫定: 引き分けは双方に勝ち点なし）

### 4.5 操作（確定）

- **キーボードのみ**（マウス不可）
- **ユニットリストを Tab で巡回** → **方向キーで移動先** を指定
- 攻撃・撤退などのコマンドキーは UI 実装 Issue で定義（Tab / 矢印 / Enter / Esc を基本）

### 4.6 戦地 5 つ（確定）

| # | 戦地（日本語） | 英語名（マップ ID 用） |
|---|----------------|------------------------|
| 1 | アキア・クリークの戦い | Aquia Creek |
| 2 | 第一次ブルランの戦い | First Bull Run (Manassas) |
| 3 | 第二次ブルランの戦い | Second Bull Run (Manassas) |
| 4 | ストーンズ川の戦い | Stones River (Murfreesboro) |
| 5 | ゲティスバーグの戦い | Gettysburg |

各マップは史実を参考に簡略化。ターン上限は史実日数の簡略値をデータで持つ。

### 4.7 セーブ（確定）

- 形式: **JSON ファイル**
- 戦闘中・キャンペーン進行を保存し、途中再開可能

### 4.8 オーディオ

- 簡易環境音 + 環境 BGM（権利フリー or 生成音）
- 初期は無音でもビルド可能にし、後から差し込み

---

## 5. フェーズ分割（実装ロードマップ）

各フェーズ = 1 つ以上の GitHub Issue（**本リポ**）。**ユーザー承認後に着手**。

### Phase 0 — リポジトリ基盤

- [x] 公開リポ作成・クローン
- [x] `plan.md` / `README.md` 初版
- [x] 設計判断 Issue #1 とユーザー確定の反映
- [x] `mise.toml` スキャフォールド（tools + wt:* + compile/build/smoke）
- [x] 最小 raylib ウィンドウ（Hello）— `src/main.cpp` + mise `compile`（g++ 直叩き）
- [x] ライセンス・`.gitignore`
- [x] raylib = `github:raysan5/raylib@5.5`（mise tools）
- [x] g++ = `github:xpack-dev-tools/gcc-xpack@15.2.0-1`（mise tools・15.2 固定）
- [x] Makefile / `scripts/fetch-raylib.sh` / `vendor/` 方式は不採用

**完了条件:** `mise run build` で空ウィンドウが起動する。  
**状態 (2026-07-22):** worktree `experimental/20260722-civilwar-feat` で mise 集約版を実装。### Phase 1 — Hex マップ MVP

- [x] hex グリッド: **テキスト + 簡易図形の併用**（`DrawPoly` + ASCII 記号）
- [x] 地形 9 種定義・描画（マップは平地/丘/川を多用、全種は少なくとも 1 セル）
- [x] キーボード: 矢印でカーソル移動（Tab 巡回は Phase 2）
- [x] 静的マップ 1 枚: Aquia Creek 簡略（`src/data/aquia_creek.hpp` 埋め込み）

**完了条件:** キーで操作でき、地形が視認できる。  
**状態 (2026-07-22):** #5 承認後、`experimental/20260722-civilwar-feat` に実装。

### Phase 2 — ユニット 1 種 + ターン

- [x] 歩兵のみ配置（北/南 各 3）
- [x] ターン交代（Enter/Space）
- [x] **Tab でユニット巡回 → 方向キーで 1 hex 移動**
- [x] 移動力 MP=3・地形コスト・川/山不可・占有 hex 不可
- [x] 陣営表示（U 青 / C 赤）

**完了条件:** 交互に歩兵を動かせる。  
**状態 (2026-07-22):** #6 承認後、experimental に実装。

### Phase 3 — 簡易戦闘

- 射程に応じた攻撃（歩兵は汎用の仮数値）
- ダメージ・撃破・負傷
- 勝敗判定の実装（§4.4 の優先度）

**完了条件:** 1 マップで勝敗または引き分けがつく。

### Phase 4 — ユニット種別拡張

- 8 種のステータスと特殊ルール
- 銃撃隊 = 射撃専門・近接弱 / 歩兵 = 汎用
- 将軍最大 3、近衛兵、輸送隊の補給
- 大砲・騎馬の射程/移動差

**完了条件:** 8 種がマップ上で機能する。

### Phase 5 — 戦地 5 + キャンペーン

- 確定 5 戦地のマップデータ
- 史実日数ベースのターン上限テーブル
- キャンペーン進行・勝利数集計・「米国統一」画面

**完了条件:** 5 戦を通してキャンペーンが完走できる。

### Phase 6 — セーブ / ロード

- **JSON** の読み書き
- タイトルから続きへ

**完了条件:** 再起動後に途中再開できる。

### Phase 7 — 音・polish・バランス

- BGM / 環境音
- キーバインド整理、メッセージ
- バランス調整

**完了条件:** プレイ可能な小規模製品レベル。

### Phase 8 — テスト体制

- 別エージェントによるユニットテスト（ロジック層を GUI から分離）
- `mise` watch でデスクトップ CI
- 失敗は `test-log.md` に記録 → 方針をユーザー承認

**完了条件:** コアロジックに自動テストがあり、watch で回る。

---

## 6. 推奨アーキテクチャ

GUI とルールを分離し、テストしやすくする。言語は **C++**。

```
src/
  main.cpp          # エントリ・raylib ループ
  app/              # 画面状態（タイトル/戦闘/キャンペーン）
  render/           # hex 描画（図形）+ テキスト/絵文字 UI
  input/            # キーボード（Tab 巡回・方向キー）
  game/
    hex.hpp         # 座標・隣接
    map.hpp         # 地形・マップ
    unit.hpp        # ユニット
    combat.hpp      # 戦闘解決
    turn.hpp        # ターン・補給・期間
    campaign.hpp    # 5 戦地進行
    save.hpp        # JSON セーブ/ロード
  data/             # マップ定義・ターン上限（JSON 等）
tests/              # ロジック単体テスト（raylib 非依存）
```

ロジックは raylib 非依存の pure C++ を目標にし、`tests/` からリンクする。

---

## 7. 設計判断サマリ（Issue #1）

| # | 論点 | 確定 |
|---|------|------|
| 1 | 銃撃隊 vs 歩兵 | 銃撃隊=射撃専門（近接弱）、歩兵=汎用 |
| 2 | 戦いの期間 | 史実日数を簡略化したターン数 |
| 3 | 勝敗優先 | ①期間切れ→引分 ②将軍0→即敗北 ③損害50%→敗北 |
| 4 | 5 戦地 | アキア・クリーク / 第一次・第二次ブルラン / ストーンズ川 / ゲティスバーグ |
| 5 | セーブ | JSON ファイル |
| 6 | hex UI | テキスト + 簡易図形の併用 |
| 7 | 言語 | C++ 主体 + raylib（C API） |
| 8 | キーボード UI | Tab でユニット巡回 → 方向キーで移動先 |
| 9 | Issue 分担 | 親=#51、実装=本リポ Phase ごと |

出典: [Issue #1 コメント](https://github.com/bluehive/civil-war-like/issues/1#issuecomment-5045821455)

---

## 8. 非目標（スコープ外）

- 3D・リアルタイム戦闘
- オンライン対戦
- 史実の完全再現・大規模 OOB
- 複雑な経済シミュレーション
- 多数の外部依存や重いアセットパイプライン
- マウス操作

---

## 9. 次のアクション

1. ~~設計判断 Issue にユーザーがコメント~~ → **反映済み**
2. ~~Phase 0 残り（mise + Hello raylib）~~ → experimental worktree で実装済み。PR 承認待ち
3. Phase 0 の実装 Issue を本リポに作成（事後記録）し、PR で main へ
4. Phase 1 以降を順次 Issue 化
---

## 10. 進捗記録

| 日付 | 内容 |
|------|------|
| 2026-07-22 | リポ作成・クローン。`plan.md` / `README.md` 初版 |
| 2026-07-22 | 設計判断 Issue #1 作成 |
| 2026-07-22 | Issue #1 のユーザー回答を `plan.md` / `README.md` に反映 |
| 2026-07-22 | Phase 0 Hello raylib を worktree `experimental/20260722-civilwar-feat` で実験実装 |
| 2026-07-22 | Phase 0 を mise tools（g++ 15.2 + raylib 5.5）+ タスク直叩きに差し替え。Makefile 廃止 |
| 2026-07-22 | Phase 1 Hex MVP（#5 承認）: axial hex・埋め込み Aquia・矢印カーソル |
| 2026-07-22 | PR #14: hex 隙間なし描画（radius=size・選択のみ枠）。UGCW マニュアル要約を plan 追記 |
