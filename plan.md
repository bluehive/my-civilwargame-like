# Civil War Like — 実装プラン

親タスク: [my-grok-task-2026#51](https://github.com/bluehive/my-grok-task-2026/issues/51)  
リポジトリ: [bluehive/civil-war-like](https://github.com/bluehive/civil-war-like)  
参考: [Ultimate General: Civil War](https://store.steampowered.com/app/502520/Ultimate_General_Civil_War/)（小規模ライク）

---

## 1. ビジョン

南北戦争をモチーフにした **ターン制・六角マップ地上戦** を、C/C++ + **raylib** で小規模実装する。

- 北軍 vs 南軍
- 5 戦地キャンペーン（勝利数が多い方が「米国統一」）
- キーボードのみ（マウス不可）
- 見た目は PNG 最小限、ASCII / 絵文字中心
- 依存は少なく、mise でビルド・ローカルテスト

---

## 2. 開発プロセス

| ルール | 内容 |
|--------|------|
| 駆動 | GitHub Issue 駆動。実装 Issue はユーザー承認後に着手 |
| ブランチ | `main` は直接編集しない。`git worktree` で作業ブランチ |
| 区切り | 区切りの良いところで PR 提案 → 承認後に `main` へマージ |
| コミット | 作業ごとに `commit` + `push`。Issue に進捗コメント必須 |
| 環境 | Linux Mint + `mise.toml`（ビルド・テスト・watch） |
| テスト CI | GitHub Actions ではなくデスクトップ。`mise` watch + `test-log.md` |
| レビュー | 熟練 SE が push をチェック。方針変更はユーザー承認 |

### 2.1 Issue 運用

1. 本 `plan.md` を分割した実装 Issue を `civil-war-like` に作成
2. ユーザーが Issue を承認
3. worktree ブランチで実装 → commit / push → Issue コメント
4. 必要なら PR → 承認 → merge

タスクボード親 Issue は `my-grok-task-2026#51`。実装トラッキングは本リポの Issue で行う（設計判断 Issue で確定）。

---

## 3. 技術方針

| 項目 | 方針（暫定） | 要確認 |
|------|--------------|--------|
| 言語 | C++ 主体 + raylib（C API） | pure C との比率 |
| 描画・入力・音 | raylib | — |
| ビルド | CMake または単一 `Makefile` + mise タスク | 初回スキャフォールド時に決定 |
| ツールチェーン | `mise.toml` でコンパイラ・タスク管理 | — |
| データ永続化 | 当面は JSON ファイル案 | SQLite 組み込みとの比較 |
| DB | 原則なし | — |
| 依存 | raylib のみを第一目標。追加は有名ライブラリのみ | — |
| グラフィック | テキスト/絵文字 + 簡易図形。PNG は必要最小限 | hex UI の見た目 |

---

## 4. ゲーム設計（概要）

### 4.1 コアループ

1. キャンペーン画面で戦地選択（または順次）
2. 戦闘マップ開始（hex）
3. ターン交代（北軍 → 南軍）
4. 各ターン: 移動 / 攻撃 / 補給 / 撤退
5. 勝敗判定
6. 結果記録 → 次戦地 or キャンペーン終了（5 戦の勝敗で統一）

### 4.2 ユニット種別（8種）

| ID | 種別 | 想定ロール（暫定） |
|----|------|--------------------|
| INF | 歩兵 | 基本戦力・近〜中距離 |
| CAV | 騎馬隊 | 高移動・突破 |
| WAG_W | 武器輸送隊 | 弾薬補給 |
| WAG_S | 物資輸送隊 | 食料・補給 |
| ART | 大砲隊 | 遠距離・低機動 |
| RFL | 銃撃隊 | 射撃特化（歩兵との差は要設計） |
| GRD | 近衛兵 | 将軍護衛・高防御 |
| GEN | 将軍 | 最大 3 人／陣営。指揮・敗北条件に関与 |

数値（移動力・射程・攻撃・防御・コスト）は実装 Issue で表として確定する。

### 4.3 マップ・地形

- **六角形（hex）** グリッド
- 地形タイプ: 山・丘・砂地・街・道路・川・平地・谷間・岩地
- 色と文字/模様で識別
- 戦地 5 つ（史実を参考に簡略マップ）

### 4.4 勝敗（暫定ルール案）

判定は毎ターン終了時にチェック。**優先順位は設計判断 Issue で確定**。

候補:
- 将軍が 0 人 → 敗北
- 総戦力の損害（死亡+負傷）が 50% 以上 → 敗北
- 期間（ターン上限）到達時 → 損害が少ない側の勝利、または引き分けルール

キャンペーン: 5 戦地で勝利数が多い側が「米国統一」。同数時のタイブレークは後で定義。

### 4.5 操作

- **キーボードのみ**（マウス不可）
- フォーカス移動 → ユニット選択 → 移動/攻撃/撤退コマンド
- 具体キーバインドは UI Issue で定義（例: 矢印/hjkl、Enter、Esc）

### 4.6 セーブ

- 戦闘中・キャンペーン進行を保存し、途中再開可能
- 形式は JSON vs 組み込み SQLite（設計判断 Issue）

### 4.7 オーディオ

- 簡易環境音 + 環境 BGM（権利フリー or 生成音）
- 初期は無音でもビルド可能にし、後から差し込み

---

## 5. フェーズ分割（実装ロードマップ）

各フェーズ = 1 つ以上の GitHub Issue。**ユーザー承認後に着手**。

### Phase 0 — リポジトリ基盤（本ドキュメント含む）

- [x] 公開リポ作成・クローン
- [x] `plan.md` / `README.md`
- [ ] 設計判断 Issue（曖昧点へのユーザーコメント）
- [ ] `mise.toml` スキャフォールド
- [ ] 最小 raylib ウィンドウ（Hello）
- [ ] ライセンス・`.gitignore`

**完了条件:** `mise run build` で空ウィンドウが起動する。

### Phase 1 — Hex マップ MVP

- hex グリッド描画（テキスト/簡易図形）
- 地形タイル表示（最低 3 種から開始し、9 種へ）
- キーボードでカーソル移動
- 単一戦地の静的マップデータ

**完了条件:** キーで hex 上を移動し、地形が視認できる。

### Phase 2 — ユニット 1 種 + ターン

- 歩兵のみ配置（北/南）
- ターン交代
- 移動（移動力・進入不可地形の基本）
- 陣営表示

**完了条件:** 交互に歩兵を動かせる。

### Phase 3 — 簡易戦闘

- 隣接（または射程 1）攻撃
- ダメージ・撃破
- 勝敗の最小実装（全撃破 or 将軍 0）

**完了条件:** 1 マップで勝敗がつく。

### Phase 4 — ユニット種別拡張

- 残りユニット種のステータスと特殊ルール
- 将軍最大 3、近衛兵、輸送隊の補給
- 大砲・騎馬の射程/移動差

**完了条件:** 8 種がマップ上で機能する。

### Phase 5 — 戦地 5 + キャンペーン

- 5 マップデータ（史実ベースの簡略図）
- キャンペーン進行・勝利数集計
- 「米国統一」画面

**完了条件:** 5 戦を通してキャンペーンが完走できる。

### Phase 6 — セーブ / ロード

- セーブデータの読み書き
- タイトルから続きへ

**完了条件:** 再起動後に途中再開できる。

### Phase 7 — 音・polish・バランス

- BGM / 環境音
- キーバインド整理、メッセージ、勝敗優先の最終化
- バランス調整

**完了条件:** プレイ可能な小規模製品レベル。

### Phase 8 — テスト体制

- 別エージェントによるユニットテスト（ロジック層を GUI から分離）
- `mise` watch でデスクトップ CI
- 失敗は `test-log.md` に記録 → 方針をユーザー承認

**完了条件:** コアロジックに自動テストがあり、watch で回る。

---

## 6. 推奨アーキテクチャ（暫定）

GUI とルールを分離し、テストしやすくする。

```
src/
  main.cpp          # エントリ・raylib ループ
  app/              # 画面状態（タイトル/戦闘/キャンペーン）
  render/           # hex 描画・テキスト UI
  input/            # キーボードコマンド
  game/
    hex.hpp         # 座標・隣接
    map.hpp         # 地形・マップ
    unit.hpp        # ユニット
    combat.hpp      # 戦闘解決
    turn.hpp        # ターン・補給
    campaign.hpp    # 5 戦地進行
    save.hpp        # セーブ/ロード
  data/             # マップ定義（テキスト or JSON）
tests/              # ロジック単体テスト
```

ロジックは raylib 非依存の pure C++ を目標にし、`tests/` からリンクする。

---

## 7. 曖昧点（設計判断）

以下は **実装前にユーザーコメントで確定** する。専用 Issue を参照。

1. 「銃撃隊」と「歩兵」の差（射程・火力・コスト）
2. 「実際の戦いの期間」のターン上限モデル
3. 勝敗の優先順位（将軍全滅 vs 損害 50% vs 期間切れ）
4. 5 戦地の選定
5. セーブ形式（JSON vs 組み込み SQLite）
6. raylib hex UI（テキスト寄り vs 簡易図形）
7. C と C++ の比率
8. マウス不可時のキーボード UI 設計
9. 親 Issue と実装 Issue のリポジトリ分担

→ Issue: [#1 設計判断が必要な点](https://github.com/bluehive/civil-war-like/issues/1)

---

## 8. 非目標（スコープ外）

- 3D・リアルタイム戦闘
- オンライン対戦
- 史実の完全再現・大規模 OOB
- 複雑な経済シミュレーション
- 多数の外部依存や重いアセットパイプライン

---

## 9. 次のアクション

1. 設計判断 Issue にユーザーがコメント
2. 回答を反映して本 `plan.md` を更新（小さな PR）
3. Phase 0 残り（mise + Hello raylib）の Issue を作成し、承認後に実装
4. Phase 1 以降を順次 Issue 化

---

## 10. 進捗記録

| 日付 | 内容 |
|------|------|
| 2026-07-22 | リポ作成・クローン。`plan.md` / `README.md` 初版 |
| 2026-07-22 | 設計判断 Issue 作成（ユーザーコメント待ち） |
